import { View } from 'react-native';
import { List } from 'lucide-react-native';

export function CategorieButton() {
    return (
        <View className={'items-center justify-center w-12 rounded-full bg-sand-1'}>
            <List color={'black'}/>
        </View>
    )
}
